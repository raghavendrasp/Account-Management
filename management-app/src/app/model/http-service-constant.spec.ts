import { HttpServiceConstant } from './http-service-constant';

describe('HttpServiceConstant', () => {
  it('should create an instance', () => {
    expect(new HttpServiceConstant()).toBeTruthy();
  });
});
